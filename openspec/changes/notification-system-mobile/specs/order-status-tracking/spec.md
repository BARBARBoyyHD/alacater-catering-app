## MODIFIED Requirements

### Requirement: Order Status Timeline
The system SHALL display a visual timeline of the order lifecycle (e.g., Paid → Preparing → Shipping → Delivered).

#### Scenario: Notify on status change
- **WHEN** an order status is updated to a new stage
- **THEN** system triggers a local or push notification to the user
