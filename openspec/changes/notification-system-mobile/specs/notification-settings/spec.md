## ADDED Requirements

### Requirement: Granular Notification Toggles
The system SHALL provide user toggles for different notification types (e.g., Order Updates, Promotions, Account Alerts).

#### Scenario: Disable promotions
- **WHEN** user toggles off "Promotions" in settings
- **THEN** system stops displaying push notifications for that category
